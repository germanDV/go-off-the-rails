---
description: Write a detailed implementation spec to the specs directory based on our discussion.
argument-hint: "{feature-name}"
---
Based on our discussion, formalize the implementation plan.

Please:
1. Read `specs/_template.md` to understand the required format.
2. Write a complete spec to `specs/$1.md` following the template.
3. Include all DB schema changes, sqlc queries, domain models, repositories, controller endpoints, views, middleware, and route registration details.
4. Number the implementation steps clearly.
5. Keep the spec as the single source of truth — it should be detailed enough that someone could implement it without further discussion.

Do NOT implement the code yet — only write the spec.
